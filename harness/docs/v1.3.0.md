---
version: 1.3.0
date: 2026-05-18
type: minor
---

# v1.3.0 — 잔여 개선건 P0+P1 일괄 진행

## 변경 요약

[[2026-05-18-12-00-residual-improvements-audit]] 에서 정리된 8 트랙 중
P0(T1+T6) 와 P1(T2+T3) 묶음을 한 세션에서 일괄 진행. 정원의 **engine 첫 실행**
부채를 청산하고, **의미 분석 도커 첫 진입**을 합류시킴.

## P0 — 즉시 봉합 묶음

### T1. KotlinAnalyzer R1/R2 회귀 봉합

| 회귀 | 상태 |
|------|------|
| R1: `class X private constructor(...) : Y(args)` inherits 누락 | ✅ 봉합 |
| R1': `abstract class X protected constructor(...) : Y<T>(...)` inherits 누락 | ✅ 봉합 (modifier prefix 확장) |
| R2: `class X : Y(args)` super-call이 잘못된 `creates` 엣지 | ✅ 봉합 (multi-line header pre-pass + skip set) |

수정:
- `Services/Analyzers/Languages/KotlinAnalyzer.cs` — `ClassDecl()` modifier prefix 확장, `ClassWithBase()` visibility-qualified constructor 흡수, 멀티라인 header pre-pass + `headerLines` skip set
- `Tests/LanguageAnalyzerTests.cs` — 3 신규 회귀 fixture (`Kotlin_PrivateConstructor_PreservesInheritance`, `Kotlin_AbstractClass_WithGenericBase_PreservesInheritance`, `Kotlin_SuperConstructorCall_IsNotCreates`)

회귀 매트릭스: Kotlin Pekko Typed `inherits` 0/5 → **5/5** ✅. `creates` false positive **3건 제거**.

### T6. Engine 첫 실행 (test-audit + actor-graph-extension)

v1.2.0 합류 후 한 번도 트리거되지 않았던 두 engine을 동기 baseline으로 실행:

| Engine | 로그 | 결과 |
|--------|------|------|
| `test-audit` | [[2026-05-18-12-30-baseline-after-r1-r2]] | 111 pass / 0 fail / 회귀 0, Kotlin 매트릭스 100% 회복 |
| `actor-graph-extension` | [[2026-05-18-12-31-after-r1-r2-baseline]] | 3-툴킷 매트릭스: inherits 15/15 ✅, spawn 1/10 (정규식 한계), imports 3/3 ✅ |

→ engine 정의가 회귀 가드로 처음 작동. 다음부터는 정기 트리거 가능.

## P1 — 다음 단계 묶음

### T2. 그래프 모델 신규 엣지 5종 (1급 시민 등록)

`EdgeKind` 가 free-form string이라 스키마 변경 없음. 변경 = **discoverability**:

- `Models/EdgeKinds.cs` (신규) — 11 const string:
  - 구조 6종: `Contains`, `Defines`, `Imports`, `InheritsOrImplements`, `Creates`, `UsesType`
  - 액터 5종: `SpawnsChild`, `ReceivesMessage`, `SendsMessageTo`, `SupervisesWith`, `ActorNamed`
- `Tests/GraphQueryParserTests.cs` — `Parse_AcceptsActorModelEdgeKinds` Theory (5 신규 엣지 round-trip 검증)

분석기 magic string은 그대로 둠 (비파괴). 의미 분석 도커가 emit 시 `EdgeKinds.SpawnsChild` 참조.

### T3. Roslyn 도커 Phase 1 PoC foundation

[[semantic-analyzer-docker]] Phase 1 의 8 체크리스트 중 6 완료:

**호스트 측 (AOT-safe)**:
- `Services/Semantic/SemanticCache.cs` — `~/.codescan/semantic/<lang>/` 캐시, sha256+toolVersion 키
- `Services/Semantic/SemanticDockerRunner.cs` — `Process.Start` 기반 docker run/pull/self-check
- `Services/Semantic/SemanticResultMerger.cs` — `JsonDocument.Parse` NDJSON → `SourceDependency`, regex+semantic Merge 정책
- `Commands/SemanticCommand.cs` — status / install / self-check / analyze / clear
- `Program.cs` — `semantic` 명령 라우팅 + PrintHelp 갱신
- `Services/AppPaths.cs` — `SemanticDir` 추가

**컨테이너 측 (격리)**:
- `docker/semantic-csharp/SemanticAnalyzer.csproj` — Roslyn Workspaces 4.13 + MSBuildLocator
- `docker/semantic-csharp/Program.cs` — `--self-check` + MSBuildWorkspace 분석 + NDJSON 출력
- `docker/semantic-csharp/Dockerfile` — 멀티스테이지 (sdk:9.0)
- `docker/semantic-csharp/README.md` — 빌드/실행/contract

**테스트**:
- `Tests/SemanticResultMergerTests.cs` — 7 케이스 (edge/node/actor-edge/malformed/Merge×2/Cache key)

**분리된 항목 (다음 단계)**:
- `SemanticProbeStrategy` 자동 합류 — standalone `codescan semantic analyze` 로 PoC 검증 후 hybrid 합류
- 이미지 빌드 + dogfooding 측정 — 사용자 도커 환경에서 실행

## 테스트 결과

```
이전 baseline (v1.2.0):    108 pass
v1.3.0 (P0+P1 묶음):       123 pass  (+15, 회귀 0)
```

신규 테스트 15건 내역:
- Kotlin R1/R1'/R2: 3
- GraphQueryParser actor-edge Theory: 5
- SemanticResultMerger: 7

## config 변경

- `version`: 1.2.0 → 1.3.0
- agents/engine: 변경 없음 (7/2 유지)
- `harness/docs/v1.3.0.md` 추가

## 트리거 등록 위치

모두 기존 에이전트/엔진 트리거에서 자연스럽게 흡수. 표준 `harness-kakashi-creator`
SKILL.md 미변경. 프로젝트 전용 행위는 모두 `harness/agents/*.md` frontmatter.

## 평가 (tamer 3축)

| 축 | v1.2.0 | v1.3.0 | 코멘트 |
|----|--------|--------|--------|
| 워크플로우 개선도 | B (도구만 있음) | **A** | engine 첫 실행 → 회귀 가드 활성 |
| Claude 스킬 활용도 | 3/5 | **4/5** | semantic-bridge-architect 가 실제 코드 합류 |
| 하네스 성숙도 | L4 | **L4 강화** | engine 로그 누적 시작, 의미 분석 첫 진입 |

## 즉시 가능한 작업

- 도커 이미지 빌드: `docker build -t codescan/semantic-csharp:latest docker/semantic-csharp`
- self-check: `codescan semantic self-check csharp`
- dogfooding: `codescan semantic analyze csharp .`
- 잔여 잔여(?): T4 (Phase 1-B Pekko Typed 매처), T5 (Phase 2~4), T7 (Orleans/Erlang fixture 확장)

## 다음 버전 후보 (v1.4.0+)

- **v1.4.0** — Roslyn 이미지 dogfooding 측정 결과 + `SemanticProbeStrategy` 자동 합류
- **v1.5.0** — Phase 1-B 액터 매처 (`spawns_child` / `receives_message` emit)
- **v2.0.0** — Phase 2 트랙(TS/Go/Python) 합류 + 3-언어 cross-toolkit fixture 확장

## 관련

- 출처 audit: [[2026-05-18-12-00-residual-improvements-audit]]
- P0 로그:
  - [[2026-05-18-12-30-baseline-after-r1-r2]] (test-audit 첫 실행)
  - [[2026-05-18-12-31-after-r1-r2-baseline]] (actor-graph-extension 첫 실행)
- P1 로그:
  - [[2026-05-18-13-00-roslyn-phase1-foundation]] (Roslyn PoC foundation)
- 이전 버전: [[v1.2.0]] (다국어 + 액터 전문가 합류)
